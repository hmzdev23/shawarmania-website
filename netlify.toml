[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  # Skip secrets scanning for public Supabase keys (they're designed to be public)
  SECRETS_SCAN_OMIT_KEYS = "NEXT_PUBLIC_SUPABASE_URL,NEXT_PUBLIC_SUPABASE_ANON_KEY"
